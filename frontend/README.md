# Compile and Conquer - Frontend

This is the React.js frontend for the Compile and Conquer platform.

## Running Locally
1. Ensure you have the Node.js backend server running first.
2. Run `npm install` to install dependencies.
3. Run `npm start` to launch the development server on `localhost:3000`.
